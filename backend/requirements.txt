flask
flask-cors
pymongo
dnspython  # required by pymongo for mongodb+srv:// URIs
python-dotenv
pillow
gunicorn
requests
google-generativeai

# Phase 4: ML analytics (hotspots / seasonal mining / priority model)
numpy
pandas
scikit-learn
joblib
opencv-python-headless

# Phase 5: YOLOv8 object detection (civic issue detection - primary detector)
# ONNX Runtime runs the exported civic_yolov8.onnx WITHOUT torch, keeping
# the backend inside low-RAM free tiers (Render = 512 MB).
onnxruntime
